---
const { section } = Astro.props
---

<div class="flex flex-col gap-6">
  <h3 class="text-color-placeholder text-sm font-medium">{section.title}</h3>
  <ul class="flex flex-col gap-4">
    {
      section.items.map((item) => (
        <li>
          <a href={item.path} class="text-color-default text-base font-medium transition-colors hover:text-color-primary">
            {item.title}
          </a>
        </li>
      ))
    }
  </ul>
  <a href={section.seeAllPath} class="mt-2 text-sm font-medium text-color-placeholder hover:underline">{section.seeAllText} &rarr;</a>
</div>
