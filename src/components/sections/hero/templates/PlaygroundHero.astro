---
import { Container } from '@components/layout'
import { Icon } from 'astro-icon/components'

import { SITE_URLS } from '@/constants/urls'
---

<section
  class="hero-section bg-color-surface text-color-default border-color-disabled relative flex border-b pt-10 pb-16 lg:pt-20 lg:pb-30"
  data-theme="light"
>
  <Container class="flex flex-1 items-start">
    <div
      class="flex h-full w-full flex-1 flex-col items-center justify-center text-center"
    >
      <!-- Title & Description -->

      <h1
        class="font-sans-tight mb-4 flex flex-col text-4xl font-bold tracking-tight sm:text-5xl lg:text-6xl"
      >
        <span>Pick a Color. Make it Yours.</span>
      </h1>
      <p class="mb-12 max-w-3xl text-lg font-normal sm:text-xl">
        Working in a team? Solo designer or developer? We’ve got you covered.
        Choose a subscription to get all collaborative features.
      </p>

      <!-- Buttons -->
      <div class="flex flex-col gap-3 sm:flex-row">
        <plus-button
          kind="outlined"
          href={SITE_URLS.figma}
          target="_blank"
          external={true}
        >
          <Icon name="logos:figma" class="h-5 w-5" />
          Preview in Figma
        </plus-button>
        <plus-button
          kind="filled"
          status="primary"
          href={SITE_URLS.main}
          target="_blank"
          external={true}
        >
          Purchase Now
          <i class="fa-solid fa-arrow-right"></i>
        </plus-button>
      </div>
    </div>
  </Container>
</section>
